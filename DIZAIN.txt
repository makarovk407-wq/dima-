/* Общие стили для всей страницы */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f8f8f8; /* Светлый фон, как у бумаги */
    color: #333;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center; /* Центрируем контент */
    min-height: 100vh; /* Занимаем всю высоту экрана */
}

/* Контейнер, ограничивающий ширину приложения */
.notepad-container {
    width: 90%;
    max-width: 800px; /* Максимальная ширина, чтобы не было слишком широко */
    padding: 20px;
    background-color: #fff; /* Белый фон для "блокнота" */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Легкая тень для объема */
    border-radius: 8px; /* Скругленные углы */
    display: flex;
    flex-direction: column; /* Элементы идут сверху вниз */
}

/* Стили для заголовка */
header {
    text-align: center;
    margin-bottom: 20px;
    border-bottom: 1px solid #eee;
    padding-bottom: 10px;
}

header h1 {
    color: #007bff; /* Синий цвет для акцента */
    margin: 0;
}

header p {
    font-size: 0.9em;
    color: #666;
}

/* Стили для основного содержимого (где находится textarea) */
main {
    flex-grow: 1; /* Позволяет main занять все доступное пространство */
    padding-bottom: 20px;
}

/* Стили для самого текстового поля (самый важный элемент) */
#note-area {
    width: 100%;
    height: 60vh; /* Занимает 60% высоты окна просмотра */
    padding: 15px;
    font-size: 1.1em;
    line-height: 1.5;
    border: 1px solid #ccc;
    border-radius: 4px;
    resize: none; /* Запрещаем пользователю менять размер поля */
    box-sizing: border-box; /* Важно, чтобы padding не увеличивал общую ширину */
    font-family: monospace; /* Моноширинный шрифт, как в редакторе */
}

/* Убираем синюю рамку при фокусе, чтобы было чище */
#note-area:focus {
    outline: none;
    border-color: #007bff;
    box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
}

/* Стили для подвала */
footer {
    text-align: center;
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid #eee;
    font-size: 0.8em;
    color: #999;
}